---
import { type CollectionEntry } from "astro:content";
import { Image } from "astro:assets";
import { getLangFromUrl } from "../i18n/utils";

type Props = {
    post: CollectionEntry<'posts'>;
};

const { post } = Astro.props;
const [lang, ...slug] = post.id.split('/');
---

<article>
    <a href={`/${lang}/blog/${slug}`}>
        <Image
            src={post.data.image}
            width={600}
            height={600 / 1.5}
            alt={post.data.title}
            class="rounded-2xl shadow-xl mb-4 aspect-thumbnail object-cover"
        />
    </a>
    <a
        href={`/${lang}/blog/${slug}`}
        class="text-4xl text-zinc-900 dark:text-zinc-50 font-semibold inline-block"
    >
        <h2>{post.data.title}</h2>
    </a>
    <div class="tags overflow-hidden">
        {post.data.categories.map((tag: string) => (
        <p class="mt-2 mb-2"><a href={`/${lang}/tags/${tag}`} class="tag rounded-2xl text-gray-50 bg-amber-600 dark:bg-emerald-500 h-9 w-fit px-2 py-1">{tag}</a></p>
        ))}
    </div>
    <p class="text-zinc-500 text-2xl line-clamp-2">
        {post.body}
    </p>
</article>